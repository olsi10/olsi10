name: GitHub-Profile-3D-Contrib

on:
  schedule: # 03:00 JST == 18:00 UTC
    - cron: "0 18 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: generate-github-profile-3d-contrib
    steps:
      - uses: actions/checkout@v2
      - uses: yoshi389111/github-profile-3d-contrib@0.6.0
        env:
          GITHUB_TOKEN: ${{ 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 }}
          USERNAME: ${{ olsi10 }}
      - name: Commit & Push
        run: |
          git config user.name olsi10
          git config user.email wangbampang2035@gmail.com
          git add -A .
          git commit -m "generated"
          git push
